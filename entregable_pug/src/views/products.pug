doctype
html
    head
        meta(charset='utf-8')
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
        title
    body
        h1(class="display-4 d-flex justify-content-center") Vista de productos
        div(class="mx-auto d-flex justify-content-center")
            if products.length>0
                table(class="table rounded table-dark" style=" width:1000px;")
                    thead
                        tr
                            th(scope="col") #
                            th(scope="col") Nombre
                            th(scope="col") Precio
                            th(scope="col") Foto
                    tbody
                        each product in products
                            tr
                                th(scope="row")= product.id
                                td= product.title
                                td= product.price
                                td: img(src= product.thumbnail alt="Product picture" style="width:100px; height:100px; border-radius: 10%")
            else
                div(class="alert alert-danger" role="alert") No se han encontrado productos